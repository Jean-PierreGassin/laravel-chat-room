{
  "name": "socket.io-chat-room",
  "version": "0.0.1",
  "description": "a socket.io app",
  "scripts": {
    "dev": "rm -rf dist && mkdir dist && mkdir dist/assets && cp src/index.html dist/index.html && cp src/assets/styles.css dist/assets/styles.css && webpack"
  },
  "dependencies": {
    "babel-core": "^6.9.1",
    "babel-loader": "^6.2.4",
    "babel-preset-es2015": "^6.9.0",
    "express": "^4.13.4",
    "socket.io": "^1.4.6",
    "socket.io-client": "^1.4.6",
    "webpack": "^1.13.1"
  }
}
